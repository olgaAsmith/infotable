'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#287eff',
      main: '#171d2c',
      dark: '#283047',
      contrastText: '#ccced3',
    },
  },
});

export default theme;
